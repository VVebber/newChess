/*

 class spam{
public:
    std::vector <sf::RectangleShape> Figure;
public:
        void CreatSquare() {
        sf::RectangleShape figure;
        figure.setSize(sf::Vector2f(60, 60));
        //figure.setOutlineThickness(1);
        //figure.setOutlineColor(sf::Color(250, 150, 100));
        figure.setPosition(16, 85);
        Figure.push_back(figure);
        }
        void sp(){
        std::cout<<spam2::Figure.size();
        }
};

 class spam2 : public spam{
 public:
     void CreatSquareRook(){
        Figure.clear();
        for (int i = 0, j = 0; i < 4; i++) {
            CreatSquare();
        }
    }
 };

 * */
